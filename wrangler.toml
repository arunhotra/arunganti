name = "twitterizer-proxy"
main = "worker.js"
compatibility_date = "2024-01-01"

# The GROQ_API_KEY will be set as a secret via CLI
# Don't put the actual key here - use: wrangler secret put GROQ_API_KEY
